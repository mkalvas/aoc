import { solutionOne, solutionTwo } from './code';

const day = {
  label: 'Lit pixels',
  path: `${__dirname}/input.txt`,
  solutionOne,
  solutionTwo,
  answers: [5259, 15287],
};

export default day;
