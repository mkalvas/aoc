import { solutionOne, solutionTwo } from './code';

const day = {
  label: 'Decoded',
  path: `${__dirname}/input.txt`,
  solutionOne,
  solutionTwo,
  answers: [237, 1009098],
};

export default day;
