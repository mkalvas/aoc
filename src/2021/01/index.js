import { solutionOne, solutionTwo } from './code';

const day = {
  label: 'Increasing Increments',
  path: `${__dirname}/input.txt`,
  solutionOne,
  solutionTwo,
  answers: [1502, 1538],
};

export default day;
