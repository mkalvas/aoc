import { solutionOne, solutionTwo } from './code';

const day = {
  label: '',
  path: `${__dirname}/input.txt`,
  solutionOne,
  solutionTwo,
  answers: [undefined, undefined],
};

export default day;
