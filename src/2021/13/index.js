import { solutionOne, solutionTwo } from './code';

const day = {
  label: 'Folds',
  path: `${__dirname}/input.txt`,
  solutionOne,
  solutionTwo,
  answers: [737, 'ZUJUAFHP'],
};

export default day;
