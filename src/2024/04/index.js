import { solutionOne, solutionTwo } from './code';

const day = {
  label: 'Search Results',
  path: `${__dirname}/input.txt`,
  solutionOne,
  solutionTwo,
  answers: [2534, 1866],
};

export default day;
