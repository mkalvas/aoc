import { solutionOne, solutionTwo } from './code';

const day = {
  label: 'Combat Score',
  path: `${__dirname}/input.txt`,
  solutionOne,
  solutionTwo,
  answers: [35005, 32751],
};

export default day;
