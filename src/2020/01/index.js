import { solutionOne, solutionTwo } from './code';

const day = {
  label: 'Multiple',
  path: `${__dirname}/input.txt`,
  solutionOne,
  solutionTwo,
};

export { findPair } from './code';
export default day;
