import { solutionOne, solutionTwo } from './code';

const day = {
  label: 'Valid Passports',
  path: `${__dirname}/input.txt`,
  solutionOne,
  solutionTwo,
  answers: [245, 133],
};

export default day;
