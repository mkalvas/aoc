import { solutionOne, solutionTwo } from './code';

const day = {
  label: 'Possible Bag Colors',
  path: `${__dirname}/input.txt`,
  solutionOne,
  solutionTwo,
  answers: [302, 4165],
};

export default day;
