import { solutionOne, solutionTwo } from './code';

const day = {
  label: 'Valid Passwords',
  path: `${__dirname}/input.txt`,
  solutionOne,
  solutionTwo,
  answers: [528, 497],
};

export default day;
