import { solutionOne, solutionTwo } from './code';

const day = {
  label: 'Counted Pairs',
  path: `${__dirname}/input.txt`,
  solutionOne,
  solutionTwo,
  answers: [651, 956],
};

export default day;
