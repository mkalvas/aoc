import { solutionOne, solutionTwo } from './code';

const day = {
  label: 'Sand Grains',
  path: `${__dirname}/input.txt`,
  solutionOne,
  solutionTwo,
  answers: [625, 25193],
};

export default day;
