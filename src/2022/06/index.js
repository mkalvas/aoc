import { solutionOne, solutionTwo } from './code';

const day = {
  label: 'Marker Index',
  path: `${__dirname}/input.txt`,
  solutionOne,
  solutionTwo,
  answers: [1855, 3256],
};

export default day;
