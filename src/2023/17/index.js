import { solutionOne, solutionTwo } from './code';

const day = {
  label: 'Heat loss',
  path: `${__dirname}/input.txt`,
  solutionOne,
  solutionTwo,
  answers: [886, 1055],
};

export default day;
