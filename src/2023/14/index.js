import { solutionOne, solutionTwo } from './code';

const day = {
  label: 'Beam load',
  path: `${__dirname}/input.txt`,
  solutionOne,
  solutionTwo,
  answers: [105982, 85175],
};

export default day;
